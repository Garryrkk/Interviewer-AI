{
  "name": "interviewer-ai",
  "version": "1.0.0",
  "main": "electron-main.js",
  "private": true,
"scripts": {
  "dev": "cd \"agenda frontend\" && npm run dev",
  "start:electron": "node start-electron.js",
  "build": "cd \"agenda frontend\" && npm run build",
  "electron:build": "electron-builder"
},
"devDependencies": {
  "concurrently": "^9.2.1",
  "electron": "^38.0.0",
  "electron-builder": "^26.0.12",
  "wait-on": "^8.0.4"
  }
}
